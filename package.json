{
  "name": "lyo-qr-json",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm ci && npm run build && npx prisma generate",
    "start": "cd backend && npm run start:prod"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}

